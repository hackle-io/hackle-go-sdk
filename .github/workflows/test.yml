name: Test

on:
  pull_request:

jobs:
  go-sdk-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.16'
      - name: Test
        run: go test -json
